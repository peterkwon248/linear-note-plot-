/** 2026-05-17 — Labels route moved from /labels to /library/labels.
 *  Rendering handled by always-mounted LabelsView in layout.
 *  Legacy /labels route still works (back-compat).
 */
export default function LibraryLabelsPage() {
  return null
}
