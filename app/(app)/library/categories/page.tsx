/** 2026-05-19 — Library Categories sub-page. Rendering handled by
 *  always-mounted WikiView in layout (wikiViewMode = "category" overview).
 *  Library sidebar 유지 + wiki entity의 categories overview UI 재사용.
 *  사용자 보고 "Categories 누르면 위키 사이드바로 옮겨감" fix.
 */
export default function LibraryCategoriesPage() {
  return null
}
