# Plot App — UI Kit

High-fidelity recreation of the Plot app. Run `index.html` to view an interactive click-through.

Components:
- `ActivityBar.jsx` — 44px left rail with nav
- `Sidebar.jsx` — 220px sidebar with favorites + teams
- `TopBar.jsx` — 52px top chrome with filter + view switcher
- `NoteList.jsx` — the list view (note rows)
- `Editor.jsx` — the note editor (Tiptap-look)
- `CommandPalette.jsx` — ⌘K modal
- `Inbox.jsx` — triage surface
- `Icons.jsx` — Plot icon set
