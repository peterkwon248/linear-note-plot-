/* @jsx React.createElement */
function CGraph({ theme }) {
  return (
    <div className="plot-app" style={{ display:'grid', placeItems:'center', width:'100%', height:'100%', color:'var(--whisper-fg)', fontSize:14 }}>
      C3 Studio OS Graph — coming next iteration
    </div>
  );
}
window.CGraph = CGraph;
